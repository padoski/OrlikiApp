import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {width: 60, borderWidth: 1, borderColor: 'black'},
  hourValue: {color: 'white'},
  pickContainer: {
    backgroundColor: 'white',
    height: 50,
    margin: 2,
    borderWidth: 2,
    borderColor: 'black',
    justifyContent: 'center',
    alignItems: 'center',
  },
  hourValueContainer: {
    backgroundColor: '#232621',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
export default styles;
